/* lemonade - make games lol */
/**
 * use it lol
 * author - mp3e / ghostedbread
 * ----------------------------------------------------------------
 * use wgdk for 3D graphics
 */
// typescript version //
type Vector2 = {
    x: number;
    y: number;
}

type Color = {
    r: number;
    g: number;
    b: number;
}

type Sprite = {
    position: Vector2;
    fallbackColor: Color;
}
function init(a) {}
function generateSprite(position: Vector2, fallbackColor: Color): void {

}